openapi: 3.0.0
info:
  title: "LMS Backend API"
  description: |
    API complète pour la plateforme LMS - Learning Management System avec intégration KLASSCI.

    ## Rate limiting (#214)

    Les routes proxy KLASSCI sont limitées par utilisateur authentifié :
      - `GET /proxy/*` (lecture) : **100 requêtes / minute / utilisateur**.
      - écritures `/proxy/evaluations/{id}/notes`, `/proxy/cours/{id}/presences`,
        `/proxy/cours/{id}/statut` : **30 requêtes / minute / utilisateur**.

    Le rôle `supradmin` (gestionnaire plateforme) est exempté.

    Chaque réponse expose les en-têtes `X-RateLimit-Limit` et
    `X-RateLimit-Remaining`. Au dépassement, l'API répond `429 Too Many
    Requests` avec `Retry-After` (secondes) et `X-RateLimit-Reset`.
  version: "1.0.0"
  contact:
    name: "LMS Team"
    email: "support@lms.local"
  license:
    name: "MIT"

servers:
  - url: "http://localhost:8000/api"
    description: "Development Server"
  - url: "https://api.lms.local/api"
    description: "Production Server"

# Security Schemes
components:
  securitySchemes:
    sanctum:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: "Sanctum token authentication"

  responses:
    # Réponse standard de dépassement du rate limit (#214/#219).
    TooManyRequests:
      description: "Rate limit dépassé (voir en-têtes X-RateLimit-* et Retry-After)."
      headers:
        Retry-After:
          schema:
            type: integer
          description: "Secondes à attendre avant de réessayer."
        X-RateLimit-Limit:
          schema:
            type: integer
          description: "Quota maximum par minute."
        X-RateLimit-Remaining:
          schema:
            type: integer
          description: "Requêtes restantes dans la fenêtre courante."
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: false
              message:
                type: string
                example: "Trop de requêtes. Veuillez réessayer plus tard."

  schemas:
    SchoolRegistrationRequestInput:
      type: object
      required:
        - nom_demandeur
        - email_demandeur
        - nom_ecole
        - usage_prevu
      properties:
        nom_demandeur:
          type: string
          maxLength: 191
          description: "Futur superAdmin de l'etablissement, si la demande est validee."
          example: "Awa Kouassi"
        email_demandeur:
          type: string
          format: email
          maxLength: 191
          example: "awa.kouassi@cabinet-kf.ci"
        telephone_demandeur:
          type: string
          maxLength: 40
          nullable: true
          example: "+225 07 00 00 00 00"
        nom_ecole:
          type: string
          maxLength: 191
          example: "Cabinet Kouassi Formation"
        slug_souhaite:
          type: string
          maxLength: 50
          nullable: true
          pattern: '^[a-z0-9\-]+$'
          description: |
            Souhait, JAMAIS une reservation. Le supradmin l'arbitre a la
            validation, collision avec un etablissement existant comprise.
          example: "cabinet-kouassi"
        usage_prevu:
          type: string
          minLength: 10
          maxLength: 2000
          description: "Texte libre. Obligatoire : il augmente le cout d'un depot automatise."
          example: "Formations comptabilite OHADA, promotions de 25 stagiaires."

    # Error Response
    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        error_code:
          type: string
          example: "UNAUTHENTICATED"
        message:
          type: string
          example: "Unauthenticated."
      required:
        - success
        - error_code
        - message

    # Auth Schemas
    LoginRequest:
      type: object
      properties:
        email:
          type: string
          format: email
          example: "user@example.com"
        password:
          type: string
          format: password
          example: "password123"
      required:
        - email
        - password

    AuthResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: object
          properties:
            token:
              type: string
              example: "token_string_here"
            user:
              type: object

    User:
      type: object
      properties:
        id:
          type: integer
          example: 1
        email:
          type: string
          example: "user@example.com"
        name:
          type: string
          example: "John Doe"
        role:
          type: string
          enum: [etudiant, enseignant, coordinateur, admin, superAdmin]

# Paths
paths:
  # ============ AUTHENTICATION ============
  /school-requests:
    post:
      tags:
        - School registration
      summary: "Deposer une demande d'ouverture d'ecole"
      description: |
        Porte d'entree du monde autonome (ADR-803-01). SEULE ecriture non
        authentifiee du systeme.

        N'ecrit qu'une ligne inerte, hors perimetre multi-tenant : ni compte,
        ni etablissement, ni reservation du `slug_souhaite`. La demande ne
        devient rien tant qu'un supradmin plateforme n'a pas tranche.

        Ne requiert PAS l'en-tete `X-Institution` : il n'y a pas encore
        d'etablissement a resoudre.

        Une seule demande en attente par adresse : un second depot met a jour
        la premiere au lieu d'en creer une seconde.

        Debit limite a 5 requetes par minute et par IP.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SchoolRegistrationRequestInput'
      responses:
        '201':
          description: |
            Demande enregistree. La reponse ne rend NI l'identifiant NI le
            statut : l'endpoint est anonyme, et les exposer offrirait de quoi
            enumerer les dossiers ou confirmer qu'une adresse a deja depose.

            Elle ne promet aucun courriel : le produit n'a aucun canal d'envoi.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: "Votre demande est enregistree. Elle sera examinee par notre equipe."
        '422':
          description: "Validation error"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '429':
          description: "Trop de demandes depuis cette adresse IP"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/login:
    post:
      tags:
        - Authentication
      summary: "Login user"
      description: "Authenticate user with email and password, returns Sanctum token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: "Login successful"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: "Invalid credentials"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: "Validation error"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/me:
    get:
      tags:
        - Authentication
      summary: "Get authenticated user"
      security:
        - sanctum: []
      responses:
        '200':
          description: "User data"
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/User'
        '401':
          description: "Unauthenticated"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/logout:
    post:
      tags:
        - Authentication
      summary: "Logout user"
      security:
        - sanctum: []
      responses:
        '200':
          description: "Logout successful"
        '401':
          description: "Unauthenticated"

  /auth/refresh:
    post:
      tags:
        - Authentication
      summary: "Refresh authentication token"
      security:
        - sanctum: []
      responses:
        '200':
          description: "New token"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: "Unauthenticated"

  # ============ PROXY ROUTES (KLASSCI) ============
  /proxy/structure:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get organizational structure"
      description: "Retrieve organizational structure (branches, levels). Rate limit : 100 req/min/utilisateur (#214)."
      security:
        - sanctum: []
      responses:
        '200':
          description: "Structure data"
        '401':
          description: "Unauthenticated"
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /proxy/classes:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get all active classes"
      security:
        - sanctum: []
      parameters:
        - name: filiere_id
          in: query
          schema:
            type: integer
        - name: niveau_id
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: "List of classes"
        '401':
          description: "Unauthenticated"

  /proxy/classes/{id}/etudiants:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get students of a class"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: annee_id
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: "List of students"
        '401':
          description: "Unauthenticated"
        '404':
          description: "Class not found"

  /proxy/matieres:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get subjects"
      security:
        - sanctum: []
      parameters:
        - name: filiere_id
          in: query
          schema:
            type: integer
        - name: niveau_id
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: "List of subjects"

  /proxy/matieres/{id}:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get subject details"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "Subject details"

  /proxy/enseignants:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get teachers"
      security:
        - sanctum: []
      responses:
        '200':
          description: "List of teachers"

  /proxy/filieres:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get branches/programs"
      security:
        - sanctum: []
      responses:
        '200':
          description: "List of branches"

  /proxy/niveaux-etudes:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get study levels"
      security:
        - sanctum: []
      responses:
        '200':
          description: "List of study levels"

  /proxy/evaluations:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get evaluations"
      security:
        - sanctum: []
      responses:
        '200':
          description: "List of evaluations"

  /proxy/emploi-temps:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Get schedule"
      security:
        - sanctum: []
      responses:
        '200':
          description: "Schedule data"

  /proxy/test-connection:
    get:
      tags:
        - Proxy / KLASSCI
      summary: "Test KLASSCI connection"
      description: "Admin/Coordinator only - Test connection to KLASSCI API"
      security:
        - sanctum: []
      responses:
        '200':
          description: "Connection successful"
        '401':
          description: "Unauthenticated"
        '403':
          description: "Insufficient permissions (requires coordinateur or admin role)"

  # ============ EVALUATIONS ============
  /evaluations:
    get:
      tags:
        - Evaluations
      summary: "List evaluations"
      security:
        - sanctum: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
        - name: per_page
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: "List of evaluations"
        '401':
          description: "Unauthenticated"

    post:
      tags:
        - Evaluations
      summary: "Create evaluation"
      security:
        - sanctum: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                description:
                  type: string
      responses:
        '201':
          description: "Evaluation created"
        '422':
          description: "Validation error"

  /evaluations/{id}:
    get:
      tags:
        - Evaluations
      summary: "Get evaluation details"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "Evaluation details"
        '404':
          description: "Evaluation not found"

    put:
      tags:
        - Evaluations
      summary: "Update evaluation"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "Evaluation updated"
        '403':
          description: "Permission denied"
        '404':
          description: "Not found"

    delete:
      tags:
        - Evaluations
      summary: "Delete evaluation"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "Evaluation deleted"
        '403':
          description: "Permission denied"
        '404':
          description: "Not found"

  # ============ DASHBOARD ============
  /dashboard/student:
    get:
      tags:
        - Dashboard
      summary: "Student dashboard"
      security:
        - sanctum: []
      responses:
        '200':
          description: "Student dashboard data"
        '401':
          description: "Unauthenticated"

  /dashboard/teacher:
    get:
      tags:
        - Dashboard
      summary: "Teacher dashboard"
      security:
        - sanctum: []
      responses:
        '200':
          description: "Teacher dashboard data"
        '401':
          description: "Unauthenticated"

  # ============ CHAPTERS ============
  /lessons/{lessonId}/chapters:
    get:
      tags:
        - Chapters
      summary: "List chapters of a lesson"
      security:
        - sanctum: []
      parameters:
        - name: lessonId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "List of chapters"

    post:
      tags:
        - Chapters
      summary: "Create chapter in a lesson"
      security:
        - sanctum: []
      parameters:
        - name: lessonId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                titre:
                  type: string
      responses:
        '201':
          description: "Chapter created"
        '422':
          description: "Validation error"

  /chapters/{id}:
    get:
      tags:
        - Chapters
      summary: "Get chapter details"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "Chapter details"
        '404':
          description: "Chapter not found"

    put:
      tags:
        - Chapters
      summary: "Update chapter"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "Chapter updated"

    delete:
      tags:
        - Chapters
      summary: "Delete chapter"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "Chapter deleted"

  # ============ NOTIFICATIONS ============
  /notifications:
    get:
      tags:
        - Notifications
      summary: "List notifications"
      security:
        - sanctum: []
      parameters:
        - name: read
          in: query
          schema:
            type: string
            enum: [true, false]
        - name: per_page
          in: query
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: "List of notifications"

  /notifications/unread-count:
    get:
      tags:
        - Notifications
      summary: "Get unread count"
      security:
        - sanctum: []
      responses:
        '200':
          description: "Unread count"

  /notifications/{id}/mark-as-read:
    post:
      tags:
        - Notifications
      summary: "Mark notification as read"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "Marked as read"

  # ============ FILES ============
  /files:
    get:
      tags:
        - Files
      summary: "List files"
      security:
        - sanctum: []
      responses:
        '200':
          description: "List of files"

    post:
      tags:
        - Files
      summary: "Upload file"
      security:
        - sanctum: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: "File uploaded"
        '422':
          description: "Validation error"

  /files/{id}/download:
    get:
      tags:
        - Files
      summary: "Download file"
      security:
        - sanctum: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "File downloaded"

  /webhooks/visio/recording-ready:
    post:
      tags:
        - Visio
      summary: "Webhook enregistrement visio prêt (#469)"
      security: []
      responses:
        '202':
          description: "Accepted for processing"
        '401':
          description: "Invalid signature"

# Security (global)
security:
  - sanctum: []

tags:
  - name: "Authentication"
    description: "User authentication and session management"
  - name: "Proxy / KLASSCI"
    description: "Routes proxying to KLASSCI API with authentication"
  - name: "Evaluations"
    description: "Student evaluations and assessments"
  - name: "Dashboard"
    description: "Dashboard endpoints for students and teachers"
  - name: "Chapters"
    description: "Course chapters and content"
  - name: "Notifications"
    description: "User notifications"
  - name: "Files"
    description: "File upload and download"
